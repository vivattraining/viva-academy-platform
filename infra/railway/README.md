# Railway

Use Railway or an equivalent service for `apps/api`.

Recommended:

- private Postgres attachment
- background jobs later for certificate generation and notification retries
- separate staging and production services

